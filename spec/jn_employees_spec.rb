require "spec_helper"

describe JnEmployees do
  it "has a version number" do
    expect(JnEmployees::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
